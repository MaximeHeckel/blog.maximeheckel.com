import { styled } from '@maximeheckel/design-system';

export const Main = styled('main', {
  isolation: 'isolate',
  backgroundColor: 'var(--gray-000)',
  boxShadow: '0 0 128px 24px rgba(0, 0, 0, 0.8)',
});
