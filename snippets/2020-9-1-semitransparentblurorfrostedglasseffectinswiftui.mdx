---
title: Semi transparent blur or frosted glass effect in SwiftUI
language: swift
date: '2020-09-01T23:09:32.183Z'
slug: 2020-9-1-semitransparentblurorfrostedglasseffectinswiftui
snippetImage: '/static/images/2020-9-1-semitransparentblurorfrostedglasseffectinswiftui.png'
type: snippet
---

```swift snippet
import SwiftUI

struct Blur: UIViewRepresentable {
    var effect: UIVisualEffect?
    func makeUIView(context: UIViewRepresentableContext<Self>) -> UIVisualEffectView { UIVisualEffectView() }
    func updateUIView(_ uiView: UIVisualEffectView, context: UIViewRepresentableContext<Self>) { uiView.effect = effect }
}

struct ContentView: View {
    var body: some View {
        ZStack {
            Circle().fill(Color.orange).frame(width: 150, height: 150)
            Blur(effect: UIBlurEffect(style: .light))

        }.background(Color.white).frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
```
